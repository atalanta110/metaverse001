// import { findRectByClick } from "../tools";

// window.onmessage = function ({ data }: { data: any }) {
//   const result = findRectByClick(data.coordinates, data.cacheData);

//   window.postMessage(result);
// };

export {};
